% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_compute.R
\name{compute_alpha}
\alias{compute_alpha}
\title{Compute alpha diversity per sample / group across ranks}
\usage{
compute_alpha(
  x,
  group_cols = NULL,
  ranks = "peptide_id",
  mode = c("binary", "threshold", "abundance"),
  abundance_col = NULL,
  threshold = NULL,
  abundance_agg = c("mean", "sum", "max"),
  metrics = .alpha_metric_names,
  shannon_base = c("ln", "log2", "log10"),
  carry_cols = NULL,
  group_interaction = FALSE,
  interaction_only = FALSE,
  interaction_sep = " * ",
  shannon_log = NULL
)
}
\arguments{
\item{x}{A \verb{<phip_data>} object or a long \code{data.frame}.}

\item{group_cols}{Character vector of grouping columns, or \code{NULL} for a single
aggregate (non-facetted) table. All columns must be present on the input.}

\item{ranks}{Character vector of \strong{exact column names} to aggregate by.
Typical values: \code{"peptide_id"} or taxonomy/lineage columns.}

\item{mode}{One of \code{"binary"} (default), \code{"threshold"}, or \code{"abundance"}.
Controls how per-category counts are computed; see \strong{Modes} in Details.}

\item{abundance_col}{Character scalar naming the abundance column. Required
for \code{mode = "abundance"}; optional for \code{mode = "threshold"} (defaults to
\code{"fold_change"}); ignored for \code{mode = "binary"}.}

\item{threshold}{Numeric scalar. Required for \code{mode = "threshold"}. Ignored
for other modes.}

\item{abundance_agg}{One of \code{"mean"} (default), \code{"sum"}, or \code{"max"}. Used
only in \code{mode = "abundance"} when \code{rank != "peptide_id"} to aggregate
peptide-level abundances within each rank category.}

\item{metrics}{Character vector of diversity metrics to compute. Any subset
of: \code{"richness"}, \code{"shannon"}, \code{"simpson"}, \code{"pielou_evenness"},
\code{"berger_parker"}. Defaults to all five. Output columns follow the
canonical names: \code{richness}, \code{shannon_diversity}, \code{simpson_diversity},
\code{pielou_evenness}, \code{berger_parker_dominance}.}

\item{shannon_base}{One of \code{"ln"}, \code{"log2"}, \code{"log10"}; reporting base for
the Shannon index (via base change from natural log).}

\item{carry_cols}{Optional character vector of extra columns to carry forward
into the output if present (e.g. sample metadata in your table).}

\item{group_interaction}{Logical; also compute the interaction of all
\code{group_cols} (default \code{FALSE}).}

\item{interaction_only}{Logical; if \code{TRUE}, return only the interaction table
(requires \code{group_interaction = TRUE} and at least two \code{group_cols}).}

\item{interaction_sep}{Separator used for the interaction label (default
\code{" * "}). The resulting string (e.g. \code{"GroupA * T1"}) becomes both a list
name and a value in the output column, so choose a separator that does not
appear in your group levels.}

\item{shannon_log}{Deprecated. Use \code{shannon_base} instead.}
}
\value{
A \strong{named list} of data frames with S3 class
\code{"phip_alpha_diversity"}. Each element (per \code{group_col}, plus optional
interaction or \code{"all_samples"}) contains: \code{rank}, \code{sample_id}, the grouping
column (or \code{group} when \code{group_cols = NULL}), any \code{carry_cols}, and one
column per requested metric (see \code{metrics}). \code{pielou_evenness} is \code{NA}
when richness <= 1; \code{berger_parker_dominance} is \code{NA} when richness == 0.
}
\description{
Computes \strong{richness}, \strong{Shannon}, \strong{Simpson}, \strong{Pielou's evenness}, and
\strong{Berger-Parker dominance} per sample and per grouping variable at one or
more \strong{ranks} (columns describing peptides).
}
\details{
\subsection{Ranks}{

Ranks are the peptide identities or characteristics you aggregate by. They
must be \strong{exact column names}:
\itemize{
\item For \verb{<phip_data>}: columns in the PHIPER peptide library (e.g. \code{peptide_id},
lineage/taxa fields).
\item For \code{data.frame}: columns present on your long count table.
}
}

\subsection{Modes}{

The \code{mode} parameter controls what \code{n} represents in every diversity formula:
\itemize{
\item \code{"binary"} (default): presence = \code{exist > 0}. \code{n} = count of distinct
enriched peptides per \verb{(sample, rank_val)}.
\item \code{"threshold"}: presence = \code{abundance_col > threshold}. \code{n} = count of
peptides passing the threshold. \code{threshold} is required; \code{abundance_col}
defaults to \code{"fold_change"} when \code{NULL}.
\item \code{"abundance"}: no binarisation. \code{abundance_col} is required. At
\code{rank = "peptide_id"}, \code{n} = the raw abundance value per peptide. At
higher ranks, peptides are aggregated within each \verb{(sample, rank_val)}
using \code{abundance_agg} (\code{"mean"}, \code{"sum"}, or \code{"max"}).
}
}

\subsection{Grouping, interactions, and interaction-only mode}{
\itemize{
\item \code{group_cols} can be a character vector; the return value is a \strong{named list}
of data frames, one per \code{group_col}.
\item If \code{group_cols = NULL}, a single non-facetted table is returned under the
name \code{"all_samples"}.
\item If \code{group_interaction = TRUE} (and you supplied >= 2 \code{group_cols}), an
additional element is computed for the interaction of all group columns,
with labels joined by \code{interaction_sep}.
\item If \code{interaction_only = TRUE}, you get \strong{only} that interaction element
(requires \code{group_interaction = TRUE} and at least 2 \code{group_cols}).
}
}
}
\examples{
pd <- load_example_data()
# phip_data input: peptide-level diversity by group
out <- compute_alpha(
  pd, group_cols = "group", ranks = "peptide_id"
)

# include interaction of multiple grouping variables
out2 <- compute_alpha(
  pd,
  group_cols = c("group", "timepoint"),
  ranks = c("peptide_id", "family", "genus"),
  group_interaction = TRUE
)

# interaction only (returns a single element named "group * timepoint")
out3 <- compute_alpha(
  pd,
  group_cols = c("group", "timepoint"),
  ranks = "peptide_id",
  group_interaction = TRUE,
  interaction_only = TRUE
)

\dontrun{
# data.frame input: ranks must be columns in the data
out_df <- compute_alpha(
  df_long, group_cols = NULL, ranks = "peptide_id"
)
}

# threshold mode: presence = fold_change > 1.5
out_thr <- compute_alpha(
  pd, group_cols = "group", ranks = "peptide_id",
  mode = "threshold", threshold = 1.5
)

}
