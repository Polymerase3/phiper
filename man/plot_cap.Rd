% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_plots.R
\name{plot_cap}
\alias{plot_cap}
\title{Plot CAP/db-RDA Results (Constrained Ordination)}
\usage{
plot_cap(
  cap_res,
  axes = c(1, 2),
  group_col = NULL,
  time_col = NULL,
  show_centroids = TRUE,
  centroid_by = c("auto", "group", "time", "group_time"),
  connect_centroids = c("none", "group", "time"),
  show_ellipses = TRUE,
  ellipse_by = c("group"),
  ellipse_type = c("t", "norm", "euclid"),
  ellipse_level = 0.95,
  point_size = 1.5,
  point_alpha = 0.6,
  centroid_size = 3
)
}
\arguments{
\item{cap_res}{A \code{"beta_capscale"} object as returned by \code{\link[=compute_capscale]{compute_capscale()}}.
Must contain at least a \code{sample_coords} tibble with columns \code{sample_id}
and \code{CAP1}, \code{CAP2}, ..., and a numeric \code{eigenvalues} vector for constrained
axes.}

\item{axes}{Integer vector of length 2 giving the CAP axes to plot
(e.g., \code{c(1, 2)} for CAP1 vs CAP2). Default is \code{c(1, 2)}.}

\item{group_col}{Optional name of a grouping column in
\code{cap_res$sample_coords} used to colour points (e.g., \code{"group"},
\code{"type_person"}). If \code{NULL}, the function auto-detects \code{"group"}
if present.}

\item{time_col}{Optional name of a \strong{categorical} time column in
\code{cap_res$sample_coords} used to shape points (e.g., \code{"time"} or
\code{"timepoint"}). If \code{NULL}, the function auto-detects \code{"time"} if present.
Continuous time is not supported in this plotting function.}

\item{show_centroids}{Logical; if \code{TRUE} (default), plots centroids for
groups / time levels / group*time combinations depending on \code{centroid_by}.}

\item{centroid_by}{How to define centroids. One of
\code{"auto"}, \code{"group"}, \code{"time"}, \code{"group_time"}. \code{"auto"} uses:
group*time if both factors are available, otherwise group if present,
otherwise time.}

\item{connect_centroids}{How to connect centroids with lines. One of
\code{"none"} (default), \code{"group"}, \code{"time"}. The logic is:
\itemize{
\item if centroids are by \code{group_time} and \code{connect_centroids = "group"},
centroids of each group are connected along time levels;
\item if centroids are by \code{group_time} and \code{connect_centroids = "time"},
centroids of each time level are connected across groups;
\item for other \code{centroid_by} values, connections are ignored.
}}

\item{show_ellipses}{Logical; if \code{TRUE} (default), draws ellipses for
selected factors.}

\item{ellipse_by}{Character vector describing which ellipses to draw.
Any combination of \code{"group"}, \code{"time"}, \code{"group_time"}, or \code{"none"}
(to disable). Default is \code{"group"}.}

\item{ellipse_type}{Ellipse type passed to \code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}},
one of \code{"t"}, \code{"norm"}, \code{"euclid"}. Default \code{"t"}.}

\item{ellipse_level}{Numeric confidence level for ellipses (default \code{0.95}).}

\item{point_size}{Numeric size of sample points. Default \code{1.5}.}

\item{point_alpha}{Numeric opacity of sample points in \verb{[0, 1]}.
Default \code{0.6}.}

\item{centroid_size}{Numeric size for centroid points. Default \code{3}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object representing the CAP ordination.
}
\description{
Creates a 2D scatter plot from a \code{"beta_capscale"} object (output of
\code{\link[=compute_capscale]{compute_capscale()}}), showing sample scores on CAP axes. Points can be
coloured by group and shaped by (categorical) time. Optional ellipses and
centroids can be overlaid for group/time/group*time summaries.
}
\details{
Axis labels are annotated with the percentage of constrained variance
explained by each CAP axis, computed as
\eqn{100 * \lambda_k / \sum_j \lambda_j}, where \eqn{\lambda_k} are the
constrained eigenvalues from \code{cap_res$eigenvalues}. Only positive parts
of eigenvalues are used when computing percentages.

The function uses \code{\link[=theme_phip]{theme_phip()}} if it exists in the search path;
otherwise it falls back to \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}}.

Typically you will want to join sample-level metadata into
\code{cap_res$sample_coords} before plotting, e.g.:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cap_res$sample_coords <- dplyr::left_join(
  cap_res$sample_coords,
  meta,             # data frame with sample_id, group, time, etc.
  by = "sample_id"
)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
  # cap_res <- compute_capscale(dist_bc, ps = ps, formula = ~ type_person + age)
  cap_res$sample_coords <- dplyr::left_join(
    cap_res$sample_coords,
    meta,  # contains type_person, timepoint, etc.
    by = "sample_id"
  )

  plot_cap(
    cap_res,
    axes            = c(1, 2),
    group_col       = "type_person",
    time_col        = "timepoint",
    show_centroids  = TRUE,
    centroid_by     = "group_time",
    connect_centroids = "group",
    show_ellipses   = TRUE,
    ellipse_by      = c("group", "group_time")
  )
}
}
