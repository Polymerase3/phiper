% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pop_compute.R
\name{compute_pop}
\alias{compute_pop}
\title{Prevalence comparison by group (POP framework)}
\usage{
compute_pop(
  x,
  rank_cols,
  group_cols,
  exist_col = "exist",
  pop_k_min = 1L,
  paired = FALSE,
  peptide_library = NULL
)
}
\arguments{
\item{x}{A \code{phip_data} object or a data.frame/tibble with at least columns
\code{sample_id}, \code{peptide_id}, \code{exist_col}, and all \code{group_cols}. If
\code{paired} is set, the paired linking column must also be present. If
\code{rank_cols} include non-peptide taxa, \code{x} must provide \code{peptide_library}.}

\item{rank_cols}{Character vector of rank columns, e.g. \code{c("peptide_id", "species")}.}

\item{group_cols}{Character vector of binary grouping columns. Each column must
have exactly 2 non-missing levels in the data.}

\item{exist_col}{Name of the binary presence column (default \code{"exist"}).}

\item{pop_k_min}{Integer >= 1; k-of-n POP threshold per sample (default 1).}

\item{paired}{\code{FALSE} (default) or a single string naming the column that
links related samples (e.g. \code{"subject_id"}). When set, paired McNemar
tests are used instead of Fisher.}

\item{peptide_library}{Optional peptide metadata table with \code{peptide_id} and
any non-peptide rank columns. If \code{NULL}, taken from \code{x$peptide_library}.}
}
\value{
A data.frame with one row per (rank, feature, group_col) comparison:
\code{rank}, \code{feature}, \code{group_col}, \code{group1}, \code{group2},
\code{n1}, \code{N1}, \code{prop1}, \code{percent1},
\code{n2}, \code{N2}, \code{prop2}, \code{percent2},
\code{ratio}, \code{delta_ratio} (unpaired only), \code{p_raw}, \code{n_peptides}.
A \code{view} column is prepended when the input carries a view attribute.
}
\description{
Computes feature-level prevalence across binary group columns and performs
pairwise statistical tests:
\itemize{
\item \strong{Unpaired:} Fisher's exact test (2x2) per (rank, feature, group pair).
\item \strong{Paired:} McNemar's exact binomial test per subject.
}

Presence per sample is determined by a k-of-n rule: a feature is considered
present in a sample if at least \code{pop_k_min} of its contributing peptides are
positive. Each \code{group_col} must have exactly 2 non-missing levels in the data.
}
\examples{
\dontrun{
res <- compute_pop(pd, rank_cols = "species", group_cols = "group")
res
}

}
