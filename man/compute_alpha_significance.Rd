% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_significance.R
\name{compute_alpha_significance}
\alias{compute_alpha_significance}
\title{Compute statistical significance of alpha diversity between groups}
\usage{
compute_alpha_significance(
  x,
  group_col = NULL,
  metric = NULL,
  global_test = c("kruskal", "anova"),
  pairwise_test = c("wilcoxon", "tukey"),
  p_adjust_method = "BH"
)
}
\arguments{
\item{x}{A \code{"phip_alpha_diversity"} list (output of \code{\link[=compute_alpha]{compute_alpha()}}) or a
single alpha-diversity data frame.}

\item{group_col}{Character scalar; name of the grouping column. Inferred
from \code{attr(x, "group_cols")} when \code{NULL} (default).}

\item{metric}{Character vector; subset of metrics to test. \code{NULL} (default)
uses all numeric metric columns present in the data.}

\item{global_test}{One of \code{"kruskal"} (Kruskal-Wallis, default) or \code{"anova"}
(one-way ANOVA).}

\item{pairwise_test}{One of \code{"wilcoxon"} (Wilcoxon rank-sum, default) or
\code{"tukey"} (Tukey HSD from \code{aov()}).}

\item{p_adjust_method}{Method passed to \code{\link[=p.adjust]{p.adjust()}}. Default \code{"BH"}.}
}
\value{
A named list of class \code{"phip_alpha_significance"} with two tibbles:
\describe{
\item{\verb{$global}}{One row per \verb{(rank, metric)}: \code{rank}, \code{metric},
\code{statistic}, \code{p_value}, \code{test}.}
\item{\verb{$pairwise}}{One row per \verb{(rank, metric, pair)}: \code{rank}, \code{metric},
\code{group1}, \code{group2}, \code{p_raw}, \code{p_adj}, \code{cohens_d}, \code{stars}, \code{test}.}
}
Attributes: \code{group_col}, \code{global_test}, \code{pairwise_test},
\code{p_adjust_method}, \code{metrics}, \code{ranks}.
}
\description{
Runs global (Kruskal-Wallis or one-way ANOVA) and pairwise (Wilcoxon or
Tukey HSD) hypothesis tests on each \verb{(rank, metric)} combination present in
the output of \code{\link[=compute_alpha]{compute_alpha()}}. Pairwise p-values are adjusted with
\code{\link[=p.adjust]{p.adjust()}} and Cohen's d effect sizes are appended.
}
\examples{
\dontrun{
alpha <- compute_alpha(phip_obj, group_cols = "group")
sig   <- compute_alpha_significance(alpha)
sig$global
sig$pairwise
}
}
