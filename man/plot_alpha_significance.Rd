% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_significance.R
\name{plot_alpha_significance}
\alias{plot_alpha_significance}
\title{Visualise alpha diversity significance results}
\usage{
plot_alpha_significance(
  x,
  metric = NULL,
  rank = NULL,
  type = c("table", "heatmap"),
  p_threshold = 0.05,
  ...
)
}
\arguments{
\item{x}{A \code{"phip_alpha_significance"} object from
\code{\link[=compute_alpha_significance]{compute_alpha_significance()}}.}

\item{metric}{Character scalar; metric to display. Defaults to the first
metric present in \code{x}.}

\item{rank}{Character scalar; rank to display. Defaults to the first rank
present in \code{x}. Ignored when the data has no \code{rank} column.}

\item{type}{One of \code{"table"} (default) or \code{"heatmap"}.}

\item{p_threshold}{Numeric; pairs with \code{p_adj > p_threshold} are shown in
grey in the heatmap and excluded from the table. Default \code{0.05}.}

\item{...}{Reserved; ignored.}
}
\value{
\itemize{
\item \code{type = "table"}: a \code{\link[tibble:tibble]{tibble::tibble}} of significant pairwise results.
\item \code{type = "heatmap"}: a \code{ggplot} heatmap (Cohen's d fill, stars overlaid).
}
}
\description{
Summarises the pairwise significance table from \code{\link[=compute_alpha_significance]{compute_alpha_significance()}}
either as a filtered tibble (\code{type = "table"}) or a Cohen's d heatmap with
significance annotations (\code{type = "heatmap"}).
}
\examples{
\dontrun{
sig <- compute_alpha_significance(alpha_list)
plot_alpha_significance(sig, type = "table")
plot_alpha_significance(sig, type = "heatmap")
}
}
