% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alpha_plots.R
\name{plot_alpha}
\alias{plot_alpha}
\title{Plot alpha diversity (richness/Shannon/Simpson) from precomputed results}
\usage{
plot_alpha(
  x,
  metric = c("richness", "shannon_diversity", "simpson_diversity", "pielou_evenness",
    "berger_parker_dominance"),
  group_col = "group",
  rank_col = "rank",
  filter_groups = NULL,
  filter_ranks = NULL,
  custom_colors = NULL,
  facet_by_rank = TRUE,
  ncol = 2,
  facet_scales = "fixed",
  interaction_only = FALSE,
  interaction_sep = NULL,
  jitter_width = 0.25,
  point_size = 1.8,
  point_alpha = 0.7,
  text_size = 12,
  font_family = "Montserrat",
  show_grids = TRUE,
  x_order = NULL,
  x_labels = NULL,
  y_range = NULL,
  x_tickangle = 0,
  significance = NULL,
  show_significance = FALSE,
  sig_p_threshold = 0.05,
  sig_step_increase = 0.05,
  sig_tip_length = 0.01,
  ...
)
}
\arguments{
\item{x}{a \code{"phip_alpha_diversity"} list (output of
\code{\link[=compute_alpha]{compute_alpha()}}) or a single alpha-diversity data frame.}

\item{metric}{one of \code{"richness"}, \code{"shannon_diversity"}, \code{"simpson_diversity"},
\code{"pielou_evenness"}, or \code{"berger_parker_dominance"}.}

\item{group_col}{name of the grouping column in the alpha table
(default \code{"group"} when \code{group_cols = NULL} in the computation step).}

\item{rank_col}{name of the rank column (default \code{"rank"}).}

\item{filter_groups}{optional character vector; keep only these group levels.}

\item{filter_ranks}{optional character vector; keep only these ranks.}

\item{custom_colors}{optional named vector of colors for groups.}

\item{facet_by_rank}{logical; facet by \code{rank_col} if multiple ranks present.}

\item{ncol}{integer; number of columns in facet wrap (default \code{2}).}

\item{facet_scales}{\code{"fixed"}, \code{"free_x"}, \code{"free_y"}, or \code{"free"}.}

\item{interaction_only}{logical; if \code{TRUE}, plot only the interaction table
(when available in \code{x}). useful when \code{compute_alpha()} was run
with \code{group_interaction = TRUE}.}

\item{interaction_sep}{character; separator used to join interaction labels.
default is taken from \code{attr(x, "interaction_sep")} if present, otherwise \code{" * "}.}

\item{jitter_width}{Numeric; horizontal jitter width for points.}

\item{point_size}{Numeric; size of jittered points.}

\item{point_alpha}{Numeric in (0,1); alpha for jittered points.}

\item{text_size}{Numeric; base text size for plot labels.}

\item{font_family}{Character; font family for plot text.}

\item{show_grids}{Logical; whether to show panel grid lines.}

\item{x_order}{Optional character vector specifying the x-axis order.}

\item{x_labels}{Optional named character vector mapping x-axis labels.}

\item{y_range}{Optional numeric length-2 vector for y-axis limits.}

\item{x_tickangle}{Numeric; x-axis label angle in degrees.}

\item{significance}{Optional \code{"phip_alpha_significance"} object from
\code{\link[=compute_alpha_significance]{compute_alpha_significance()}}; used to add significance brackets when
\code{show_significance = TRUE}.}

\item{show_significance}{Logical; if \code{TRUE} and \code{significance} is supplied,
add pairwise significance brackets via \code{ggsignif} (package must be
installed). Default \code{FALSE}.}

\item{sig_p_threshold}{Numeric; only pairs with \code{p_adj <= sig_p_threshold}
receive a bracket. Default \code{0.05}.}

\item{sig_step_increase}{Numeric; vertical step between stacked brackets.
Passed to \code{\link[ggsignif:stat_signif]{ggsignif::geom_signif()}}. Default \code{0.05}.}

\item{sig_tip_length}{Numeric; length of bracket tips. Default \code{0.01}.}

\item{...}{Reserved for future extensions; ignored.}
}
\value{
a \code{ggplot} object.
}
\description{
Plot alpha diversity metrics from the precomputed output of
\code{\link[=compute_alpha]{compute_alpha()}}. Supports filtering groups/ranks and optional
faceting by rank. If the input contains an interaction table, you can request
only that via \code{interaction_only = TRUE}.
}
\details{
\itemize{
\item \code{x} can be:
\itemize{
\item the named list returned by \code{\link[=compute_alpha]{compute_alpha()}} (class
\code{"phip_alpha_diversity"}), or
\item a single data frame taken from that list.
}
\item when \code{interaction_only = TRUE}, the function tries to select the element
named by \code{paste(attr(x, "group_cols"), collapse = interaction_sep)}. if not
found, it falls back to the first element whose name contains the separator.
}
}
\examples{
\dontrun{
# precomputed alpha (list) -> boxplot per group
p <- plot_alpha(alpha_list, metric = "richness", group_col = "Cohort")

# only the interaction table (if available)
p_int <- plot_alpha(
  alpha_list,
  metric = "shannon_diversity",
  group_col = "Cohort * timepoint",
  interaction_only = TRUE
)
}
}
