% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/beta_plots.R
\name{plot_scree}
\alias{plot_scree}
\title{Scree Plot for PCoA Eigenvalues}
\usage{
plot_scree(pcoa_res, n_axes = NULL, type = c("bar", "line"))
}
\arguments{
\item{pcoa_res}{A \code{"beta_pcoa"} object as returned by \code{\link[=compute_pcoa]{compute_pcoa()}}.
Must contain an \code{eigenvalues} component (numeric vector of eigenvalues).}

\item{n_axes}{Integer giving the number of axes to display. If \code{NULL}
(default), the function uses \code{min(10, length(eigenvalues))}.}

\item{type}{Type of scree plot to draw: \code{"bar"} (default) for a bar plot
of variance explained, or \code{"line"} for a line/point plot.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object representing the scree plot.
}
\description{
Creates a scree plot from a \code{"beta_pcoa"} object (output of
\code{\link[=compute_pcoa]{compute_pcoa()}}), showing the percentage of variance explained
for the first \code{n_axes} axes.
}
\details{
Percentages are computed from the positive part of the eigenvalues,
i.e. \code{pmax(eigenvalues, 0)}, to handle possible small negative
eigenvalues from non-perfectly Euclidean distance matrices.

The function uses \code{\link[=theme_phip]{theme_phip()}} if it exists in the search path;
otherwise it falls back to \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}}.
}
\examples{
\dontrun{
  # pcoa_res <- compute_pcoa(dist_bc)
  plot_scree(pcoa_res)

  # More axes as line plot:
  plot_scree(pcoa_res, n_axes = 15, type = "line")
}
}
