% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary_beta_plots.R
\name{plot_dispersion}
\alias{plot_dispersion}
\title{Plot beta dispersion (distance to centroid)}
\usage{
plot_dispersion(
  x,
  scope = "group",
  contrast = "<global>",
  show_violin = TRUE,
  show_box = TRUE,
  show_points = TRUE,
  point_size = 1.5,
  point_alpha = 0.6,
  width_violin = 0.9,
  width_box = 0.5,
  jitter_width = 0.1,
  jitter_height = 0,
  add_pvalue = TRUE
)
}
\arguments{
\item{x}{A \code{beta_dispersion} object returned by
\code{compute_dispersion()}.}

\item{scope}{Character scalar indicating which dispersion scope to plot.
Typically one of \code{"group"}, \code{"time"} or \code{"group:time"}.
Must match values in \code{x$distances$scope}. Default \code{"group"}.}

\item{contrast}{Character scalar indicating which contrast within
\code{scope} to plot, e.g. \code{"<global>"} or \code{"A vs B"}.
Must match values in \code{x$distances$contrast}. Default \code{"<global>"}.}

\item{show_violin}{Logical; if \code{TRUE} (default) draw a violin layer.}

\item{show_box}{Logical; if \code{TRUE} (default) draw hollow boxplots
(\code{fill = NA}) per level.}

\item{show_points}{Logical; if \code{TRUE} (default) draw jittered points.}

\item{point_size}{Numeric point size for jitter layer. Default 1.5.}

\item{point_alpha}{Numeric alpha for jitter layer. Default 0.6.}

\item{width_violin}{Width of violin layer. Default 0.9.}

\item{width_box}{Width of boxplot layer. Default 0.5.}

\item{jitter_width, jitter_height}{Jitter width/height for points.
Defaults: \code{0.1} and \code{0}.}

\item{add_pvalue}{Logical; if \code{TRUE} (default) and a matching row is
found in \code{x$tests}, the p-value is shown in the subtitle.}
}
\value{
A \code{ggplot} object.
}
\description{
Plot distances to group/time centroids from a \code{beta_dispersion} object
(output of \code{compute_dispersion()}) as violins, hollow boxplots and/or
jittered points, by level on the x-axis.
}
\examples{
\dontrun{
  disp_res <- compute_dispersion(dist_bc, ps, group_col = "group_char")

  # Global group dispersion:
  plot_dispersion(disp_res, scope = "group", contrast = "<global>")

  # Pairwise contrast, with only boxplots + points:
  plot_dispersion(
    disp_res,
    scope        = "group",
    contrast     = "dementia vs control",
    show_violin  = FALSE,
    show_box     = TRUE,
    show_points  = TRUE
  )
}
}
