% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binary_beta_plots.R
\name{plot_pcoa}
\alias{plot_pcoa}
\title{Plot Principal Coordinates Analysis (PCoA)}
\usage{
plot_pcoa(
  pcoa_res,
  axes = c(1, 2),
  group_col = NULL,
  time_col = NULL,
  show_centroids = TRUE,
  centroid_by = c("auto", "group", "time", "group_time"),
  connect_centroids = c("none", "group", "time"),
  show_ellipses = TRUE,
  ellipse_by = c("group"),
  ellipse_type = c("t", "norm", "euclid"),
  ellipse_level = 0.95,
  point_size = 1.5,
  point_alpha = 0.6,
  centroid_size = 3
)
}
\arguments{
\item{pcoa_res}{A \code{"beta_pcoa"} object as returned by \code{\link[=compute_pcoa]{compute_pcoa()}}.
Must contain at least a \code{sample_coords} component with columns
\code{sample_id} and \code{PCoA1}, \code{PCoA2}, …, and a \code{var_explained} component
(one-row tibble) with columns such as \code{"\%PCoA1"}, \code{"\%PCoA2"}, etc.}

\item{axes}{Integer vector of length 2 giving the PCoA axes to plot
(e.g., \code{c(1, 2)} for PCoA1 vs PCoA2). Defaults to \code{c(1, 2)}.}

\item{group_col}{Optional name of the grouping column in
\code{pcoa_res$sample_coords} (e.g., \code{"group"}, \code{"type_person"}). Used for
colouring points and for group-level centroids / ellipses. If \code{NULL},
the function attempts to auto-detect \code{"group"} if present.}

\item{time_col}{Optional name of a \strong{categorical} time column in
\code{pcoa_res$sample_coords} (e.g., \code{"time"} or \code{"timepoint"}). Used for
shaping points and for time/interaction centroids / ellipses. If \code{NULL},
the function attempts to auto-detect \code{"time"} if present. Continuous
time is \strong{not} supported here.}

\item{show_centroids}{Logical; if \code{TRUE} (default), centroids of the
chosen grouping are shown.}

\item{centroid_by}{Granularity of centroids. One of:
\itemize{
\item \code{"auto"} (default): if both \code{group_col} and \code{time_col} are
present, centroids are computed for each group–time combination
(\code{"group_time"}); otherwise by \code{"group"} or \code{"time"} depending on
which factor is available.
\item \code{"group"}: centroids per group (requires \code{group_col}).
\item \code{"time"}: centroids per time level (requires \code{time_col}).
\item \code{"group_time"}: centroids per group×time combination
(requires both \code{group_col} and \code{time_col}).
}}

\item{connect_centroids}{Character; if centroids are shown and
\code{centroid_by = "group_time"}, this controls how they are joined by
paths:
\itemize{
\item \code{"none"} (default): no connecting paths;
\item \code{"group"}: connect centroids along time within each group;
\item \code{"time"}: connect centroids across groups within each time
level.
}
For other \code{centroid_by} values, connections are ignored.}

\item{show_ellipses}{Logical; if \code{TRUE} (default), confidence ellipses
(via \code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}}) are drawn for selected factors.}

\item{ellipse_by}{Character vector specifying which factor(s) to use for
ellipses. Possible values are \code{"group"}, \code{"time"}, \code{"group_time"}.
Default is \code{"group"}. Values that require a missing factor are ignored.}

\item{ellipse_type}{Type of ellipse passed to
\code{\link[ggplot2:stat_ellipse]{ggplot2::stat_ellipse()}}, one of \code{"t"}, \code{"norm"}, \code{"euclid"}.
Defaults to \code{"t"}.}

\item{ellipse_level}{Numeric confidence level for ellipses (default
\code{0.95}).}

\item{point_size}{Numeric size of points. Default \code{1.5}.}

\item{point_alpha}{Numeric alpha (opacity) of points in \verb{[0, 1]}.
Default \code{0.6}.}

\item{centroid_size}{Numeric size of centroid points. Default \code{3}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object representing the PCoA scatter plot.
}
\description{
Creates a 2D PCoA scatterplot from a \code{"beta_pcoa"} object (the output of
\code{\link[=compute_pcoa]{compute_pcoa()}}), with optional grouping by a categorical variable,
optional time factor, centroids, centroid trajectories and ellipses.
}
\details{
The function expects that any grouping / time variables you wish to use
(e.g., \code{"type_person"}, \code{"age_group"}, \code{"timepoint"}) have already been
merged into \code{pcoa_res$sample_coords} (typically by joining with a metadata
table via \code{sample_id}).

Axis labels are automatically annotated with the percentage of variance
explained, using the \code{var_explained} component of \code{pcoa_res} if available.

The function uses \code{\link[=theme_phip]{theme_phip()}} if it exists in the search path; otherwise
it falls back to \code{\link[ggplot2:ggtheme]{ggplot2::theme_minimal()}}.
}
\examples{
\dontrun{
  # pcoa_res <- compute_pcoa(dist_bc)
  # Suppose you have metadata with columns sample_id, type_person, timepoint:
  meta_sc <- dplyr::left_join(
    pcoa_res$sample_coords,
    meta,
    by = "sample_id"
  )
  pcoa_res$sample_coords <- meta_sc

  # Basic PCoA coloured by type_person with group-level ellipses:
  plot_pcoa(
    pcoa_res,
    axes        = c(1, 2),
    group_col   = "type_person",
    time_col    = "timepoint",
    ellipse_by  = "group",
    show_centroids = TRUE,
    centroid_by    = "group_time",
    connect_centroids = "group"
  )
}
}
