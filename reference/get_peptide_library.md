# Retrieve the peptide-library annotation table

Quick accessor for the `peptide_library` slot of a **phip_data** object.

## Usage

``` r
get_peptide_library(x)
```

## Arguments

- x:

  A valid `phip_data` object.

## Value

A tibble with one row per `peptide_id` and associated annotation.
